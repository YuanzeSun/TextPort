{
  "name": "textport",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:create": "wrangler d1 create textport_db",
    "d1:migrate:local": "wrangler d1 migrations apply textport_db --local",
    "d1:migrate:remote": "wrangler d1 migrations apply textport_db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
